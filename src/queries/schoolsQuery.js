
const schoolsQuery = {
    dataSet: {
        resource: `organisationUnits/Jj1IUjjPaWf`,
        params: {
            fields: "children[name,id]",
            paging: "true"
        }
    }
  };
  export default schoolsQuery;

  